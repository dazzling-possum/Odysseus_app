<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    ===================================================================
    The manifest is the app's "table of contents". It declares:
      * Permissions the app needs (here: INTERNET).
      * That we allow plain HTTP traffic (for the local Tailscale URL).
      * Every Activity (screen) and which one is the launcher.

    NOTE: android:usesCleartextTraffic="true" allows unencrypted
    http:// URLs. This is required to reach the local Odysseus service
    over the LAN / Tailscale, which is not served over HTTPS.
    ===================================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The WebView needs to reach the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Odysseus"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- MAIN screen: hosts the WebView. This is the launcher. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Optional login screen, shown before the WebView if enabled. -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Settings screen to edit the saved URL and toggle login. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

    </application>

</manifest>
