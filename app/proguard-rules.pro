# proguard-rules.pro
# Code shrinking/obfuscation is disabled in this prototype (see
# build.gradle.kts -> isMinifyEnabled = false), so no custom rules
# are needed yet. This file is kept so the release build type can
# reference it without error.
